import express from "express";
import bodyParser, { urlencoded } from "body-Parser";
import r from "./router.js";

app = Express()
app.bodyParser("bodyParser")
app.use(expreddss.urlencoded({extended:false}))
app.use(bodyParser.json())

